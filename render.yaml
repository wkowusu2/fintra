services:
  - type: web
    name: fintra-backend
    env: docker
    buildCommand: "./mvnw clean install"
    startCommand: "java -jar target/*.jar"
    plan: free
    envVars:
      - key: SPRING_PROFILES_ACTIVE
        value: prod
